[project]
name = "cockpit-deep-agents-planning"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "deepagents==0.7.11",
    "langchain-openai>=0.3",
    "langgraph>=0.3",
    "langsmith>=0.2",
]

[tool.uv]
dev-dependencies = [
    "langgraph-cli[inmem]>=0.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
