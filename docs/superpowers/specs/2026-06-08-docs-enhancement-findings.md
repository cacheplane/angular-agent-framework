# Docs Enhancement — Findings Report

> Phase 1 output of the docs enhancement assessment. Backlog is GATED: the user
> selects what enters Phase 2. Default: P0+P1 in, P2 deferred.
> `ACCURACY-FOLLOWUP:` findings are routed to separate tasks, NOT folded into enhancement PRs.

Audit: 16 read-only agents across all 7 published libs (105 mdx) + cross-cutting parity/terminology/journey. 98 findings total (87 enhancement, 11 accuracy follow-ups).

## Summary

| Lib | P0 | P1 | P2 | Accuracy-followups |
|---|---|---|---|---|
| langgraph | 0 | 2 | 9 | 5 |
| chat | 0 | 8 | 17 | 4 |
| render | 0 | 2 | 4 | 2 |
| ag-ui | 0 | 6 | 5 | 0 |
| a2ui | 0 | 2 | 3 | 0 |
| telemetry | 0 | 2 | 2 | 0 |
| licensing | 0 | 1 | 4 | 0 |
| cross-cutting | 2 | 9 | 9 | 0 |
| **TOTAL** | **2** | **32** | **53** | **11** |

Severity: P0 = broken/misleading for a reader · P1 = high-value improvement · P2 = nice-to-have polish. Effort: S <15m · M 15–45m · L >45m.

## Findings by lib

### langgraph

| # | Page | Dim | Sev | Eff | Recommendation |
|---|---|---|---|---|---|
| E01 | `guides/deployment.mdx` | examples | P1 | S | The Stream recovery example (line ~257-275) calls `this.chat.joinStream(savedRunId, lastEventId)` but `lastEventId` is never declared or shown being persisted, leaving readers unsure where it comes from. The agent's `joinStream(id, last)` takes an optional last-event id; either show storing it alongside the run id in the `onRunCreated`/event loop, or pass `undefined` with a one-line note that LangGraph replays from the start of the run when no last-event id is supplied. |
| E02 | `guides/subgraphs.mdx` | clarity | P1 | M | Two later code blocks switch from the documented `injectAgent()` pattern to undefined service-injection patterns with no explanation: `orchestrator = inject(OrchestratorService).resource` (Subagent progress UI, line ~204) and the implied `OrchestratorState`/`PipelineState` types used throughout are never defined. Either replace `inject(OrchestratorService).resource` with `injectAgent<OrchestratorState>()` to match the rest of the guide, or add a short note + a minimal `OrchestratorService` definition so the snippet is self-contained and consistent. |
| E03 | `api/mock-stream-transport.mdx` | clarity | P2 | S | The complete test example emits values events shaped as `{ type: 'values', values: { messages: [...] } }`, but the Testing guide and the MockAgentTransport source JSDoc both use the flatter `{ type: 'values', messages: [...] }`. Both happen to work (extractEventData falls back to remaining keys), but the divergence confuses readers copying between pages. Pick one canonical shape (recommend the flat `{ type: 'values', messages: [...] }` used by the guide and source) and use it consistently here, or add a one-line note that both the nested `values` and flat `messages` forms are accepted. |
| E04 | `concepts/agent-architecture.mdx` | examples | P2 | S | In 'Building a Debug Timeline' (lines 570-606) the example reads `checkpoint.node`, `checkpoint.timestamp`, and `checkpoint.values` off `history()` entries, but never states the shape of `AgentCheckpoint`. Add one short interface block (or a sentence) documenting the `AgentCheckpoint` fields the example relies on (id, node, timestamp, values) so readers can write the binding correctly instead of guessing. |
| E05 | `concepts/angular-signals.mdx` | gaps | P2 | S | The 'What you see' tab (lines 62-76) and lifecycle examples use an unexplained `ChatState` type parameter (`injectAgent<ChatState>()`) that is never defined anywhere on the page. Add a 3-4 line `interface ChatState { messages: BaseMessage[] }` block the first time `injectAgent<ChatState>()` appears, or link to State Management where the T-parameter mapping is explained, so the generic isn't load-bearing-but-undefined. |
| E06 | `concepts/state-management.mdx` | clarity | P2 | S | The 'How Partial Updates Arrive' section (lines 208-222) says `injectAgent()` requests the stream modes it needs by default, then shows `submit(..., { streamMode: ['messages-tuple'] })` as the example of overriding — but never lists what the defaults actually are, so a reader cannot tell what they'd be giving up by narrowing to a single mode. Add a one-line note naming the default stream modes injectAgent requests (e.g. values + messages-tuple) before showing the override. |
| E07 | `getting-started/introduction.mdx` | structure | P2 | S | The Introduction's 'What's Next' grid links to Lifecycle Signals (/docs/langgraph/guides/lifecycle) but the page never introduces or shows `injectAgent().lifecycle` anywhere in its body, so the card is the only mention. Either add a one-sentence teaser for lifecycle signals in the 'Key Concepts' area, or drop the card — currently it dangles. (The agent-contract page documents `.lifecycle`, so a cross-link target exists.) |
| E08 | `guides/deployment.mdx` | structure | P2 | S | The What's Next CardGroup includes a self-link card titled 'Error Handling' pointing to `/docs/langgraph/guides/deployment` (this same page). Replace it with a real destination (e.g. the Streaming guide's error-handling section or the Testing guide) or drop the card. |
| E09 | `guides/memory.mdx` | examples | P2 | S | Both Python tabs reference an undefined `parse_json(...)` helper (and `uuid4()` is used in save_memories without an import). Add a one-line definition or import comment (e.g. `from uuid import uuid4` and a note that `parse_json` is your own JSON-extraction helper / `json.loads`) so readers copying the agent code aren't left with NameErrors. |
| E10 | `guides/testing.mdx` | gaps | P2 | M | The guide leads with `provideFakeAgent()` as the recommended first choice but never shows a runnable spec using it (TestBed setup + assertion on streamed tokens) — it only shows the app.config wiring. Add a short Jasmine/Vitest spec that wires `provideFakeAgent({ tokens: ['Hello', ' world'] })` in TestBed, runs the agent, and asserts `chat.messages()` accumulates the tokens, so the 'reach for this first' double has a copy-paste example on par with the MockAgentTransport sections. |
| E11 | `guides/time-travel.mdx` | clarity | P2 | S | The Replaying-with-modified-input component (line ~259) introduces `readonly agent = inject(AgentService).agent;` — an `AgentService` wrapper that appears nowhere else in the LangGraph docs and is never defined. Switch it to `protected readonly agent = injectAgent<AgentState>();` to match every other snippet on the page and avoid implying a service-wrapper convention readers don't have. |

### chat

| # | Page | Dim | Sev | Eff | Recommendation |
|---|---|---|---|---|---|
| E12 | `a2ui/surface-component.mdx` | gaps | P1 | M | The Inputs table lists only `surface` and `catalog`, and Outputs lists only `events`, but the real component (libs/chat/src/lib/a2ui/surface.component.ts) also has the inputs `state` (A2uiSurfaceState — the progressive-state path), `handlers` (consumer A2UI action handlers, merged with built-in a2ui:event/a2ui:localAction), and `surfaceFallback`, plus an `action` output that emits `A2uiActionMessage`. Add `state`, `handlers`, and `surfaceFallback` rows to the Inputs table and an `action: A2uiActionMessage` row to the Outputs table, describing each. This is the canonical reference page and the overview/catalog pages already use `[handlers]` and `(action)`, so the omission is a real reader trap. |
| E13 | `a2ui/surface-component.mdx` | examples | P1 | M | The 'Usage Outside ChatComponent' example renders `<a2ui-surface [surface]=... [catalog]=...>` but never wires `[handlers]` or `(action)`/`(events)`, so a reader cannot see how to receive an agent-bound action message or run a local handler. Add a second example (or extend this one) showing `<a2ui-surface [surface]="surface" [catalog]="catalog" [handlers]="handlers" (action)="sendToAgent($event)" (events)="onEvent($event)" />` with a handlers object and an action handler method, mirroring the pattern shown in a2ui/overview.mdx's 'Local Handlers' section. |
| E14 | `components/chat-trace.mdx` | examples | P1 | M | After fixing the fabricated `steps()` example, add one runnable example that wires `<chat-trace>` to the real `agent.toolCalls()` signal (the 'When to Use It' section promises pairing traces with agent tool calls but no example shows how to derive TraceState from a `ToolCall.status` of 'pending'\|'running'\|'complete'\|'error'). Show the status->TraceState mapping explicitly since the names differ ('complete' vs 'done'). |
| E15 | `components/chat.mdx` | gaps | P1 | M | The Inputs table lists `store` as type `StateStore` but that type is never explained or linked, and the only `[store]` mention has no usage example. Add a one-paragraph note (or cross-link to the Generative UI guide) explaining what a StateStore is, when interactive GenUI specs require it, and a minimal `[store]` binding snippet — otherwise readers cannot act on the input. |
| E16 | `getting-started/changelog.mdx` | drift | P1 | M | The changelog contains a single `0.0.19` entry while the published `@threadplane/chat` package is at 0.0.49 (libs/chat/package.json). Either backfill entries for the releases between 0.0.19 and 0.0.49 (at minimum a 0.0.49 entry summarizing notable additions since 0.0.19 — e.g. citations primitives, A2UI catalog, chat-sidenav/chat-popup/chat-sidebar layout compositions, regenerate() on the Agent contract) or add a note explaining the changelog only tracks selected highlight releases. As written, a reader cannot tell what changed in the version they actually installed. |
| E17 | `getting-started/installation.mdx` | clarity | P1 | M | Section 3 and section 4 each present a separate, contradictory `app.config.ts`: section 3 registers `provideAgent({ apiUrl })` + `provideChat({ license, assistantName })` globally; section 4 then re-declares `app.config.ts` with only `provideAgent({ assistantId, threadId: signal(null) })` (no apiUrl, no provideChat) AND wires `provideAgent` again at the component level in the chat-page snippet. A reader copy-pasting both ends up with two different configs and a double-registered agent. Consolidate into a single canonical `app.config.ts` (assistantId + apiUrl + threadId + provideChat) and have section 4 only show the component template binding `[agent]="chatAgent"` from `injectAgent()`, not a second provider block. |
| E18 | `guides/custom-catalogs.mdx` | examples | P1 | S | In the 'Writing a Custom Component' example, `readonly spec = input.required<Spec>();` uses the `Spec` type with no import, and `Spec` is NOT exported from `@threadplane/render`'s public API (only `AngularComponentInputs`, which contains `spec: Spec`, is exported). The snippet will not compile as written. Fix: add the import line `import { input } from '@angular/core';` plus `import type { AngularComponentInputs } from '@threadplane/render';` and either type spec via `input.required<AngularComponentInputs['spec']>()` or show the component implementing `AngularComponentInputs` rather than hand-listing fields with an unimportable `Spec`. |
| E19 | `guides/generative-ui.mdx` | gaps | P1 | M | Document the `[handlers]` input on `<chat>`. It is a real third generative-UI input (`readonly handlers = input<Record<string, (params) => unknown \| Promise<unknown>>>({})` in chat.component.ts, passed down to the render pipeline) and configuration.mdx already tells readers 'Generative UI is controlled by the ChatComponent [views], [store], and [handlers] inputs' — but no guide explains `[handlers]`. Add a 'Event Handlers' section after 'State Store' showing how a spec's `on` event bindings invoke a named handler from this map, with a copy-paste example (e.g. `[handlers]="{ submitForm: onSubmit }"`). |
| E20 | `a2ui/catalog.mdx` | structure | P2 | S | The Layout section is split awkwardly: Row/Column/Card/List appear under 'Layout Components', then Tabs and Modal appear far below under a separate 'Layout Components (continued)' heading after the Interactive section. Move Tabs and Modal up to sit with the other Layout components (or add a cross-note), so a reader scanning by category finds all six layout components together rather than discovering two of them buried after sliders and date pickers. |
| E21 | `a2ui/catalog.mdx` | gaps | P2 | S | The Button example shows `childKeys` for its content, but overview.mdx's Button example uses a singular `child: "submit_label"` field, and the catalog's Button prop table only documents `childKeys`. Clarify in the Button section whether the catalog Button accepts `child` (singular) or `childKeys` (array) — or both — referencing the actual A2uiButtonComponent input names, so authors writing surfaces aren't guessing between two shapes presented across the two pages. |
| E22 | `api/content-classifier.mdx` | examples | P2 | S | The interface lists `a2uiSurfaces` and `a2uiSurfaceStates` signals and the ContentType table includes `a2ui`, but the Usage section only demonstrates the `markdown` and `json-render` paths. Add a short usage snippet feeding an `---a2ui_JSON---`-prefixed snapshot and reading `classifier.type()` (=> 'a2ui') and `classifier.a2uiSurfaces()`, so the documented A2UI signals have a concrete worked example rather than only an interface entry. |
| E23 | `api/mock-agent.mdx` | gaps | P2 | M | The mock exposes a `lifecycle` signal and an `_internal.streamStartedAt` writable escape hatch (per libs/chat/src/lib/testing/mock-agent.ts) that tests use to drive stream-start timing without a full submit/stream cycle, plus the `withInterrupt`/`withSubagents`/`history` options produce optional signals tests must null-check. Add a short 'Lifecycle and history' subsection showing `agent._internal.streamStartedAt.set(Date.now())` and accessing `agent.interrupt?.set(...)` / `agent.history?.set(...)`, so readers know how to exercise the optional surfaces these options unlock. |
| E24 | `api/provide-chat.mdx` | structure | P2 | S | The 'What's Next' CardGroup links to /docs/chat/guides/generative-ui and /docs/chat/guides/configuration. Verify both targets exist; if either guide page is absent, replace the dead card(s) with valid destinations (e.g. the A2UI overview or the ChatConfig reference) so the page doesn't ship broken cross-links. |
| E25 | `components/chat-debug.mdx` | structure | P2 | S | Add a short closing cross-links section pointing to the Timeline/time-travel concept and to `chat-sidenav` (referenced in 'Sidenav Integration' but never linked). Readers arriving at replay/fork hooks have no path to the broader time-travel docs. |
| E26 | `components/chat-interrupt-panel.mdx` | gaps | P2 | M | The `handleInterrupt` switch handles `'edit'`/`'respond'` by calling `this.openEditor()` / `this.focusInput()`, but neither helper is defined or explained, leaving readers unsure how to resume after edit/respond. Add a brief note (or stub bodies) showing how an edited proposal or text response is sent back via `agent.submit({ resume: ... })`, since the panel itself does not mutate the agent. |
| E27 | `components/chat-message-list.mdx` | examples | P2 | S | The 'Working with Message Content' snippet calls `isString(message.content)` but `isString` is never defined, and the Full Example wires a `DomSanitizer`/`renderMd` but never demonstrates the `index` context variable highlighted earlier. Define `isString` (or inline `typeof ... === 'string'`) in the snippet so it compiles as shown. |
| E28 | `components/chat-subagent-card.mdx` | drift | P2 | S | The Subagent property table omits two members that exist on the real type (libs/chat/src/lib/agent/subagent.ts): `name?: string` and `state: Signal<Record<string, unknown>>`. The card's 'Subagent label' even references the name. Add both rows so the documented type matches the exported `Subagent` interface. |
| E29 | `components/chat-tool-call-card.mdx` | structure | P2 | S | The page has no closing cross-links/next-steps. Add a 'See also' section linking to `chat-tool-calls` (which renders many cards and is where this card is normally used) and `chat-tool-call-template` (per-tool customization), mirroring the See-also pattern used on chat-select.mdx. |
| E30 | `components/chat-tool-call-card.mdx` | examples | P2 | S | Both Basic-usage snippets bind `[toolCall]="tc"` but `tc` is never defined. Add a tiny component-class snippet showing a `ToolCallInfo` literal (e.g. `tc = { id: 'call_1', name: 'search_web', args: { query: 'angular' }, status: 'complete', result: [...] }`) so the example is copy-paste runnable. |
| E31 | `components/chat.mdx` | examples | P2 | S | The five Outputs (`regenerate`, `rate`, `messageCopy`, `renderEvent`, `threadSelected`) are documented in the table but only `threadSelected` has a usage example. Add a short snippet binding `(regenerate)`, `(rate)` and `(messageCopy)` showing the handler signatures (e.g. `rate` payload `{ messageIndex, rating }`) so consumers know how to wire message feedback/regeneration. |
| E32 | `concepts/message-model.mdx` | drift | P2 | S | The Subagents section says 'Each `Subagent` has a tool-call id, optional name, status signal, message signal, and state signal.' The actual `Subagent` interface (libs/chat/src/lib/agent/subagent.ts) exposes `messages` (plural, `Signal<Message[]>`), not a singular 'message signal'. Change 'message signal' to 'messages signal' to match the contract a reader will type against. |
| E33 | `concepts/message-model.mdx` | examples | P2 | M | The Citations section references `extractCitations()` from `@threadplane/langgraph` and says markdown views 'can render citation markers against the message citation list' but shows no example of the resolve path. Add a short snippet showing how citations reach the UI — e.g. injecting `CitationsResolverService` (exported from @threadplane/chat) or pointing to the `[^id]` marker convention from the Citation interface doc comment — so a reader building a custom citation renderer has a concrete starting point rather than just type shapes. |
| E34 | `concepts/primitives-vs-compositions.mdx` | gaps | P2 | S | The Compositions section mentions `<chat-sidenav>` with `[agent]` and `[debug]` inputs (line ~59) but `chat-sidenav` is never introduced in the composition list above (which only names `<chat>` and `<chat-debug>`), and there is no components/chat-sidenav.mdx to cross-link to. Either add `<chat-sidenav>` (and the related `<chat-popup>`/`<chat-sidebar>` layout compositions exported from public-api.ts) to the composition overview with a one-line purpose each and a link to the Layout Modes guide, or drop the orphaned `chat-sidenav` reference so the page doesn't name a component it never explains. |
| E35 | `guides/custom-catalogs.mdx` | structure | P2 | S | Add a 'What's Next' / cross-link block at the end of custom-catalogs.mdx (it currently ends abruptly after 'Writing a Custom Component' with no onward links). Link to guides/generative-ui.mdx (where `[views]` setup lives), the render specs reference (/docs/render/guides/specs), and the A2UI overview, mirroring the CardGroup footers used on generative-ui.mdx and streaming.mdx. |
| E36 | `guides/streaming.mdx` | clarity | P2 | S | The 'Content Classification' trigger table only lists `{` → json-render and 'any other text' → markdown, but the `ContentType` union and the Signals table both include `'mixed'` (and `'a2ui'`) with no explanation of when a message becomes `mixed`. Add a row or short paragraph stating that `mixed` arises when a message interleaves prose and inline JSON-render specs (markdown path with embedded specs), so readers seeing `classifier.type() === 'mixed'` understand it. A2UI is explained later, but `mixed` is never defined. |

### render

| # | Page | Dim | Sev | Eff | Recommendation |
|---|---|---|---|---|---|
| E37 | `api/define-angular-registry.mdx` | examples | P1 | M | Add a Usage example showing the per-component fallback object form, e.g. defineAngularRegistry({ Text: TextComponent, Card: { component: CardComponent, fallback: CardSkeletonComponent } }), then registry.getFallback('Card') returns CardSkeletonComponent and registry.getFallback('Text') returns the library DefaultFallbackComponent. Currently getFallback is documented as a method but no example shows how to populate a non-default fallback, so the feature is undiscoverable from this page. |
| E38 | `guides/specs.mdx` | examples | P1 | M | The $fn prop expression is documented (specs.mdx 'Computed Function' section) and the `functions` input is referenced on render-spec and in concepts/json-render-vs-a2ui.mdx, but no page shows how to register a ComputedFunction. Add a worked example to the $fn section in specs.mdx: a concrete `functions = { uppercase: (args) => String(args.text).toUpperCase() }` map (typed as Record<string, ComputedFunction> from @json-render/core) and the matching `<render-spec [functions]="functions">` / `provideRender({ functions })` wiring, so a reader can actually make a $fn expression resolve. Without this, $fn is referenced but never explained end-to-end. |
| E39 | `api/views.mdx` | examples | P2 | S | The views()/withViews()/overrideViews()/withoutViews() functions accept the RenderViewEntry object form ({ component, fallback }) in addition to bare component Types (per libs/render/src/lib/views.ts), but every example uses only bare Types. Add one example showing the object form, e.g. views({ 'plan-checklist': { component: PlanChecklistComponent, fallback: PlanSkeletonComponent } }), so view-registry consumers learn fallbacks are available here too. |
| E40 | `api/views.mdx` | structure | P2 | S | Add a Related/next-steps section at the end of views.mdx (the only render API page lacking one). Cross-link to api/define-angular-registry.mdx (the low-level registry toRenderRegistry converts to), api/provide-render.mdx, and the chat views guide, mirroring the Related blocks on the other four api pages. |
| E41 | `concepts/json-render-vs-a2ui.mdx` | structure | P2 | S | The page introduces the `views()` / `withViews()` view-registry API (lines 129-141) as the registry mechanism, but every getting-started/guide page teaches `defineAngularRegistry()` instead, and neither the registry guide nor any guide explains the relationship between `views`/`ViewRegistry` and `AngularRegistry` (toRenderRegistry converts one to the other). Add a one-line cross-link from this concept page to the Registry guide and a sentence clarifying that `views()` produces a ViewRegistry consumed by chat catalogs while `defineAngularRegistry()` produces the AngularRegistry consumed directly by <render-spec>, so readers aren't left guessing which factory to use. |
| E42 | `guides/events.mdx` | gaps | P2 | M | The events guide covers spec `on`/handler dispatch but never mentions the `events` output on RenderSpecComponent (the RenderEvent stream: RenderHandlerEvent \| RenderStateChangeEvent \| RenderLifecycleEvent, exported from public-api). The lifecycle page links back to this Events page as the source of that stream, but the link lands on a page that doesn't define it. Add a short 'Observing render events' section documenting `(events)="onEvent($event)"` on <render-spec>, the three RenderEvent variants and their `type` discriminants, and a cross-link to the Lifecycle guide. |

### ag-ui

| # | Page | Dim | Sev | Eff | Recommendation |
|---|---|---|---|---|---|
| E43 | `api/inject-agent.mdx` | gaps | P1 | S | The 'Runtime-neutral surface' table omits the public `events$: Observable<AgentEvent>` field, which IS part of the neutral `Agent` contract (libs/chat/src/lib/agent/agent.ts line 54) and is the surface the Event Mapping reference page points readers to for custom events ('CUSTOM (other names) -> events$'). Add an `events$` row to the table describing it as the runtime-neutral Observable of transient events (state_update / custom), and add one sentence distinguishing it from the AG-UI-specific `customEvents()` signal documented just below (events$ = neutral observable for side-effects; customEvents() = AG-UI signal accumulating CustomStreamEvent[] per run). Without this, a reader who follows the event-mapping cross-link finds `events$` referenced but never defined on the very API page that documents the agent surface. |
| E44 | `api/inject-agent.mdx` | examples | P1 | S | The opening example shows `readonly chat = injectAgent();` then `await this.chat.submit(...)` but never shows the agent being handed to a rendering component, so a reader never learns how injectAgent() connects to UI. Add a short copy-paste component example matching the canonical app (examples/ag-ui/angular/src/app/app.ts): a standalone component that injects the agent and binds it into `<chat [agent]="agent">` from @threadplane/chat, so readers see the end-to-end wire-up rather than an isolated submit() call. |
| E45 | `concepts/architecture.mdx` | gaps | P1 | M | provideAgent() accepts a config FACTORY form — provideAgent(() => AgentConfig) — that runs inside an injection context and may call inject() to read route params or services (see libs/ag-ui/src/lib/provide-agent.ts:38-48). This is the cleanest answer to the 'Recreate the provider for a new threadId' / route-param scenario the page raises in 'Provider choices' (lines 122-125), but the docs only ever show the static-object form. Add a short subsection under 'Provider choices' showing provideAgent(() => ({ url, threadId: inject(ActivatedRoute).snapshot.params['threadId'] })), and reference it from the threadId-recreation bullet. |
| E46 | `guides/testing.mdx` | examples | P1 | M | The headline 'Fake backend: provideFakeAgent()' section shows app.config + component wiring but never an actual runnable test. Add a copy-paste TestBed spec: configureTestingModule({ providers: [provideFakeAgent({ tokens: ['Hello'], delayMs: 0 })] }), render ChatHost via TestBed.createComponent, drive agent.submit({ message: 'hi' }), flush the timers, and assert agent.messages() contains the streamed assistant content. The mockAgent() section already has an assertion example; the fake-backend section (the more adapter-specific double) should too. |
| E47 | `guides/testing.mdx` | gaps | P1 | M | FakeAgent only emits RUN/REASONING/TEXT events — it never emits TOOL_CALL_*, STATE_SNAPSHOT/DELTA, or CUSTOM events (libs/ag-ui/src/lib/testing/fake-agent.ts). So neither test double documented here can exercise tool-call rendering, citations, or customEvents through the real reducer. Add a third subsection 'Testing tool calls, state, and custom events' showing how to subclass AbstractAgent (or use toAgent(new MyEventScriptAgent())) to script a TOOL_CALL_START/ARGS/END or STATE_SNAPSHOT sequence and assert agent.toolCalls()/agent.state()/agent.customEvents(). This is the only path to test the adapter's headline non-text features. |
| E48 | `reference/event-mapping.mdx` | clarity | P1 | S | The page presents two distinct custom-event surfaces without explaining their relationship: the summary table and 'Custom events' section route CUSTOM events to `events$` (emitting `{ type: 'custom', name, data }` / `{ type: 'state_update', data }`), while to-agent.mdx and inject-agent.mdx document the `customEvents()` signal returning `CustomStreamEvent[]` (`{ name, data }`). In reality the reducer (libs/ag-ui/src/lib/reducer.ts ~line 282-286) writes EVERY non-interrupt CUSTOM event to BOTH the customEvents signal AND events$. Add a 2-3 sentence note in the 'Custom events' section stating that each CUSTOM event is fanned out to both surfaces, when to use each (signal = accumulated per-run snapshot for generative UI like a2ui-partial; events$ = transient stream for side-effects/telemetry), and cross-link to inject-agent#ag-ui-specific-surface. |
| E49 | `api/to-agent.mdx` | examples | P2 | S | The page documents that toAgent() returns an AgUiAgent with a `customEvents()` signal and that CUSTOM events surface here, but gives no example of reading customEvents after a run. Add a tiny 3-4 line snippet showing, after constructing `const agent = toAgent(source)`, reading `agent.customEvents()` (e.g. logging or effect()) so the direct-use path has a concrete read example to mirror the inject-agent cast example. |
| E50 | `getting-started/installation.mdx` | gaps | P2 | S | AgentConfig exposes a `telemetry?: AgentRuntimeTelemetrySink \| false` option (libs/ag-ui/src/lib/provide-agent.ts:21) that is exported and wired through toAgent, but the provideAgent options table (lines 46-54) omits it. Add a `telemetry` row to that table (and the matching table in concepts/architecture.mdx 'Provider choices') noting it is opt-in and emits no telemetry unless supplied, with a one-line cross-link to the telemetry lib. |
| E51 | `getting-started/quickstart.mdx` | clarity | P2 | S | The 'Switching backends' diff (lines 78-81) shows `- provideAgent({ apiUrl })` vs `+ provideAgent({ url })` with both lines labeled `provideAgent`, implying one symbol whose key changed. They are two different functions from two different packages (@threadplane/langgraph vs @threadplane/ag-ui). Annotate the diff with the differing import line (e.g. show `import { provideAgent } from '@threadplane/langgraph'` vs `'@threadplane/ag-ui'`) or add a one-sentence note that the import source also changes, so readers don't assume a single provideAgent accepts both apiUrl and url. |
| E52 | `guides/custom-events.mdx` | clarity | P2 | S | The opening sentence (line 3) says CUSTOM events accumulate 'into the customEvents signal on the AgUiAgent returned by injectAgent()', but injectAgent() is typed to return the neutral Agent (libs/ag-ui/src/lib/provide-agent.ts:65) — the page itself later correctly casts `injectAgent() as AgUiAgent` (lines 52, 60). Reword the intro to say the signal lives on the widened AgUiAgent type and that consumers cast the injectAgent() result, so the lead doesn't contradict the cast shown 50 lines down. |
| E53 | `reference/event-mapping.mdx` | clarity | P2 | S | The 'Unsupported protocol areas' section says 'The current adapter does not implement customer-facing flows for interrupts...' but the summary table and the inject-agent page both document a working interrupt path (CUSTOM name `on_interrupt` -> `interrupt` signal, plus submit({ resume }) resume). Reword the unsupported note to scope it accurately, e.g. 'beyond the on_interrupt -> interrupt mapping documented above, there is no richer interrupt UI flow (subagents, history, time-travel)', so the two sections don't contradict each other. |

### a2ui

| # | Page | Dim | Sev | Eff | Recommendation |
|---|---|---|---|---|---|
| E54 | `getting-started/quickstart.mdx` | examples | P1 | M | The 'Build the data model' section (lines 57-73) tells the reader that turning `contents` into an object is their job and that the lib ships no `contents`->object reducer, but then builds the model with hardcoded literal calls (`setByPointer(model, '/origin', 'LAX')`) that are disconnected from the `contents` array just parsed. Add a small copy-paste reducer that actually walks `dataModelUpdate.contents` — branching on `valueString`/`valueNumber`/`valueBoolean` and recursing on `valueMap` — exactly as chat's `entriesToObject` does (libs/chat/src/lib/a2ui/surface-store.ts:48-57). This closes the loop from the parsed stream to the model instead of re-typing the values by hand. |
| E55 | `guides/data-model.mdx` | gaps | P1 | M | The guide covers the three pointer helpers and `resolveDynamic`, but never shows how to assemble a model from a `dataModelUpdate` — the single most common real task. Add a 'From contents to model' section with a runnable `entriesToObject` reducer (valueString/valueNumber/valueBoolean + recursive valueMap, mirroring libs/chat/src/lib/a2ui/surface-store.ts:48-57) and show how to honor the envelope's optional top-level `path` by writing the reduced object with `setByPointer(model, path, obj)`. Today the `path` field is mentioned in quickstart/message-protocol/schema as 'scopes where entries land' but no page shows the path+setByPointer wiring. |
| E56 | `guides/message-protocol.mdx` | gaps | P2 | S | The dataModelUpdate section (lines 75-81) names `valueMap` as 'a nested array of entries' and shows only a flat string/number example. Add one JSON example with a `valueMap` entry plus the resulting nested object (e.g. `{key:'customer', valueMap:[{key:'name',valueString:'Ada'}]}` -> `{customer:{name:'Ada'}}`) so readers see how nesting and the path: '/customer' scoping compose. Also briefly state the shape of `metadata.a2uiClientDataModel` (referenced at line 114 but never shown — it's `{version:'v1', surfaces:Record<surfaceId,Record<string,unknown>>}` per A2uiClientDataModel) or cross-link to schema.mdx for it. |
| E57 | `reference/parser-resolver-guards.mdx` | drift | P2 | S | Under resolveDynamic, the page states 'Without a scope, a relative path is treated as root-relative by prefixing /' but does not note the precedence rule that literal wrappers are unwrapped BEFORE a `{path}` check, so a value carrying both keys resolves as a literal. Add one sentence on resolution order (literals checked first, then path, then passthrough) to match resolve.ts:46-56 and the data-model guide's numbered list, so the two reference points agree. |
| E58 | `reference/schema.mdx` | gaps | P2 | M | The component definitions table (lines 68-87) omits fields that exist on the exported interfaces, which makes it incomplete as a reference: Text's `usageHint` has a fixed enum ('h1'..'h5'\|'caption'\|'body'), TextField's `textFieldType` enum is ('date'\|'longText'\|'number'\|'shortText'\|'obscured'), Row's `alignment`/`distribution` and Column/List enums, and Tabs uses `A2uiTabItem` ({title, child}) not a raw field. Add the enum value sets (at least for usageHint and textFieldType) so authors emit valid values instead of guessing. Source: libs/a2ui/src/lib/types.ts:49-158. |

### telemetry

| # | Page | Dim | Sev | Eff | Recommendation |
|---|---|---|---|---|---|
| E59 | `guides/browser.mdx` | gaps | P1 | M | browser.mdx documents only manual capture calls (telemetry.captureStreamStarted(), etc.), but the real-world path is the agent runtime emitting these events automatically through an AgentRuntimeTelemetrySink. Add a section 'Wire the runtime to telemetry' showing the canonical pattern from examples/chat/angular: pass telemetry to provideAgent (langgraph option `telemetry?: AgentRuntimeTelemetrySink \| false` on agent.provider.ts) and bridge that sink into ThreadplaneTelemetryService.capture(), mirroring examples/chat/angular/src/app/shell/runtime-telemetry.ts (createCanonicalDemoRuntimeTelemetrySink). Without this, readers think they must hand-call every capture method. |
| E60 | `guides/browser.mdx` | examples | P1 | S | The 'Configure' section uses endpoint: '/api/telemetry' and shows the JSON payload the endpoint receives, but there is no example of an endpoint handler. Add a minimal server-route example (e.g. an Angular/Node POST handler or framework API route) that reads { event, distinctId, properties } and forwards or stores it, so readers can complete the endpoint delivery path rather than guessing the request shape. |
| E61 | `getting-started/introduction.mdx` | structure | P2 | S | introduction.mdx ends on 'What this package does not promise' with no next-steps or cross-links. Add a short 'Next steps' list linking to guides/browser.mdx, guides/node.mdx, guides/privacy-and-opt-out.mdx, and reference/events.mdx so readers have a clear path out of the overview. |
| E62 | `guides/node.mdx` | gaps | P2 | S | node.mdx lists capture helpers but never states who calls them in a real deployment (server adapters / runtime code) versus app code. Add a one-paragraph 'When to call these' note clarifying that captureRuntimeInstanceCreated/captureStreamStarted are typically invoked by server-side runtime/adapter code (not application request handlers), and that disableTelemetry() must run before any capture in that process. This frames the helpers concretely. |

### licensing

| # | Page | Dim | Sev | Eff | Recommendation |
|---|---|---|---|---|---|
| E63 | `guides/setup.mdx` | gaps | P1 | S | Setup tells readers to pass `environment.threadplaneLicense` / `process.env['THREADPLANE_LICENSE']` but never says where a license token comes from. Add a short 'Getting a token' section (a sentence + link to https://threadplane.ai/pricing) at the top of setup.mdx so a reader has a path from 'I need a license' to 'here is the token to paste'. The nag messages in nag.ts already point to that URL; surface it in the docs. |
| E64 | `getting-started/introduction.mdx` | gaps | P2 | S | The 'Public API shape' table lists the functions and `LICENSE_PUBLIC_KEY` but omits the exported supporting types (`VerifyResult`/`VerifyReason`, `EvaluateResult`/`EvaluateOptions`, `RunLicenseCheckOptions`, `EmitNagOptions`) that the api reference documents. Add a one-line pointer at the end of the table — e.g. 'See the API reference for the full type surface' linking to reference/api.mdx — so the overview doesn't read as the complete export list. |
| E65 | `getting-started/introduction.mdx` | structure | P2 | S | The introduction has no next-steps / cross-links. Add a closing 'Next steps' line linking to guides/setup.mdx (consume via provideChat or direct use) and reference/api.mdx (full type surface), so readers landing on the intro have an obvious path forward. |
| E66 | `guides/ci-and-offline.mdx` | examples | P2 | S | The Offline verification, CI runLicenseCheck, and signing snippets reference bare `token`/`publicKey`/`privateKey`/`claims` variables with no setup, so none are copy-paste runnable. Make at least the offline-verification example self-contained: add the import of `LICENSE_PUBLIC_KEY`, set `const publicKey = LICENSE_PUBLIC_KEY;`, and source `token` from `process.env['THREADPLANE_LICENSE']` so the snippet runs as-is. |
| E67 | `guides/ci-and-offline.mdx` | examples | P2 | M | The `signLicense()` example (`const token = await signLicense(claims, privateKey)`) never shows how to obtain `privateKey: Uint8Array` or a matching `claims` object, making it too abstract to run for the stated minting/test use case. Add a minimal runnable snippet: build a `LicenseClaims` literal (sub/tier/iat/exp/seats) and generate a keypair via `@noble/ed25519` (`ed.utils.randomPrivateKey()` + `ed.getPublicKeyAsync()`), with a one-line note that the public key is what `verifyLicense()` consumes. |

## Cross-cutting findings

| # | Lib/Page | Dim | Sev | Eff | Recommendation |
|---|---|---|---|---|---|
| X68 | chat / `getting-started/quickstart.mdx` | gaps | P0 | S | Step 2 shows provideChat({ assistantName: 'Assistant' }) with NO license token, and the quickstart never mentions licensing at all -- yet installation.mdx makes license activation step 2 of 5 and the introduction frames chat as a commercial library with a THREADPLANE_LICENSE. A reader who enters via quickstart (the 5-minute path) ships without ever learning a license exists, then sees a [threadplane] license missing console warning with zero context. Add a one-line callout in quickstart: 'Evaluating? No license needed. Shipping commercially? See Installation for license activation -> /docs/chat/getting-started/installation'. |
| X69 | chat / `getting-started/quickstart.mdx` | structure | P0 | M | Chat has no 'go to production' page in its journey -- the deepest 'What's Next' handoffs from quickstart/layout-modes/theming all loop among theming/layout/components. Production concerns specific to chat (baking THREADPLANE_LICENSE in at build time, license console warnings in prod, swapping mockAgent for a real adapter) are scattered into installation.mdx, while agent deployment lives only in langgraph/guides/deployment. A reader on the chat track has no single 'ship chat to production' path. Add a 'Going to production' card in the quickstart/layout-modes 'What's Next' that points to langgraph/guides/deployment for the agent side and back to installation.mdx#2 for build-time license injection. |
| X70 | licensing / `getting-started/introduction.mdx` | gaps | P1 | M | Licensing is the only lib of the 7 with no getting-started/quickstart.mdx — every peer (langgraph, chat, render, ag-ui, a2ui) has one. The getting-started folder holds only introduction.mdx. Add getting-started/quickstart.mdx with a single runnable round-trip a reader can paste into a Node REPL: generate an Ed25519 keypair, signLicense() a claims object, verifyLicense() it against the public key, then evaluateLicense() to show a 'licensed' status (and a second call with a past exp to show 'grace'/'expired'). The API surface (signLicense/verifyLicense/evaluateLicense/runLicenseCheck) makes a self-contained 15-line example trivial, but no page currently strings the calls together end to end. |
| X71 | render / `concepts/json-render-vs-a2ui.mdx` | structure | P1 | S | This render-side page discusses chat's content classification, the `---a2ui_JSON---` detection path, `a2uiBasicCatalog()`, and `<a2ui-surface>` in depth but contains zero outbound doc links. Add cross-links to the chat consumer pages it describes: chat's A2UI overview (/docs/chat/a2ui/overview) for the detection/surface path and chat's generative-ui guide (/docs/chat/guides/generative-ui) for the json-render path. A reader who lands here to decide between the two renderers has no link to the place that actually wires either into chat. |
| X72 | ag-ui / `concepts/architecture.mdx` | structure | P1 | S | The 'Live a2ui streaming via customEvents' section (and the parallel passages in guides/custom-events.mdx and api/inject-agent.mdx) tell the reader that customEvents drives 'progressive a2ui surface rendering' / 'live generative-UI streaming', but none of these passages link to the chat A2UI guide (/docs/chat/a2ui/overview) where that surface rendering is actually consumed and wired. Add a cross-link from these ag-ui a2ui/generative-UI passages to chat's A2UI overview so the AG-UI reader can reach the consuming side. This is the ag-ui <-> chat (generative-ui/a2ui) link the audit asks about. |
| X73 | langgraph / `getting-started/quickstart.mdx` | gaps | P1 | S | The quickstart's Step 2 provideAgent() config uses threadId: signal(localStorage.getItem('threadId')) and onThreadId, introducing thread persistence wiring before the reader has any mental model of threads/checkpoints (that concept is only introduced later in guides/persistence). A first-run reader hits localStorage plumbing with no explanation of what a threadId is or why it can be null. Either strip threadId/onThreadId from the quickstart provider (leave just apiUrl + assistantId, matching the simpler installation.mdx example) or add a one-line callout 'threadId persistence is optional here -- see Persistence' so the minimal path stays minimal. |
| X74 | langgraph / `getting-started/quickstart.mdx` | structure | P1 | S | Missing handoff into the chat library. The quickstart teaches the hand-rolled @for (msg of chat.messages()) template and never tells the reader that @threadplane/chat exists to render this for them with <chat [agent]>. A reader who finishes the langgraph quickstart has no signpost to the chat lib -- the 'Next steps' CardGroup links only to streaming/persistence/interrupts/signals/api. Add a card 'Drop-in chat UI -> /docs/chat/getting-started/quickstart' so the two highest-traffic libs are connected at the exact point the reader is deciding whether to build UI by hand. |
| X75 | chat / `getting-started/quickstart.mdx` | gaps | P1 | S | Step 2 registers provideAgent({ apiUrl }) at the app level AND Step 3 re-registers a second provideAgent({ assistantId, threadId }) at the component level, with no explanation that this is hierarchical-DI override (the apiUrl from the root provider merges with the component-level assistantId). A reader reasonably assumes one of the two is a mistake/duplicate. Either collapse to a single provider, or add a sentence pointing to the installation.mdx 'Multiple agents' callout explaining why config is split across two provideAgent() calls. |
| X76 | chat / `getting-started/quickstart.mdx` | gaps | P1 | M | introduction.mdx and installation.mdx both promise a no-backend path ('use mockAgent() from @threadplane/chat to wire up the UI first'), but the quickstart -- the page a reader lands on to get something running fast -- requires a live LangGraph server at localhost:2024 with assistantId 'chat' and shows no mockAgent() variant. The reader most likely to want the 5-minute path is exactly the one without a backend yet. Add a Tab or short section showing <chat [agent]> bound to mockAgent() so the quickstart actually runs with zero backend. |
| X77 | chat / `guides/streaming.mdx` | structure | P1 | M | The chat streaming guide is entirely about ContentClassifier/ParseTreeStore/JSON-render internals and assumes the reader already knows what a JSON-render spec and A2UI are -- concepts not introduced anywhere in the getting-started flow. A reader following quickstart -> 'Streaming & interrupts' (the card installation.mdx points to) expects token-by-token message streaming basics and instead lands in partial-JSON parsing. Add an opening section (or a callout linking to langgraph/guides/streaming) that covers the common case 'how do assistant messages stream into <chat>' before diving into the classification pipeline, OR rename the card/handoff so readers know this page is advanced generative-UI plumbing. |
| X78 | langgraph / `guides/deployment.mdx` | gaps | P1 | M | The production error-boundary and retry examples assume APIs the getting-started flow never introduced and that don't match the documented surface: errorMessage() branches on err instanceof HttpErrorResponse, but installation.mdx Troubleshooting and streaming.mdx both say the transport uses the LangGraph SDK client (not Angular HttpClient) and tell readers to check error().cause for HTTP status -- so HttpErrorResponse will essentially never match. Additionally retry() calls this.chat.reload() and the backoff helper references a bare agent() type, neither of which appears in quickstart/streaming/persistence. Reconcile the error-shape guidance (HttpErrorResponse vs error().cause) and ensure reload()/joinStream()/streamResumable are introduced or linked from the API reference before this page relies on them. |
| X79 | licensing / `guides/setup.mdx` | examples | P2 | S | setup.mdx shows only isolated, non-runnable fragments (a bare provideChat({license}) line, a runLicenseCheck() call with undefined process.env values, an emitNag() bullet list). There is no example a reader can actually execute and observe output from. Add a complete runnable snippet for the 'Direct use' section: a real token string (or an inline signLicense() to mint one), the runLicenseCheck() call, and the logged LicenseStatus result, so the 'returns the evaluated LicenseStatus' claim is demonstrated, not just stated. |
| X80 | telemetry / `guides/browser.mdx` | examples | P2 | M | The browser guide shows a provider config and individual capture() convenience-method snippets, but no end-to-end runnable example — there is no minimal component that injects the telemetry service, calls one capture method on a user action, and shows a sink receiving the event payload. Peers (langgraph/ag-ui testing, render quickstart) all provide a paste-and-run loop. Add one self-contained example: provideThreadplaneTelemetry({enabled:true, sink}) wired in app.config, a component that calls telemetry.captureStreamStarted() on click, and the sink logging the resulting {event, properties} so a reader sees the full path fire. |
| X81 | telemetry / `guides/node.mdx` | gaps | P2 | S | node.mdx documents a CaptureResult type ({sent:true} \| {sent:false; reason}) and says 'Use the result in tests or diagnostics,' but ships zero testing example — unlike langgraph/guides/testing.mdx and ag-ui/guides/testing.mdx which both give full runnable test scaffolds. Add a short runnable example that calls disableTelemetry() then asserts captureEvent() returns {sent:false, reason:'disabled'}, so the test-facing CaptureResult contract the page advertises has a concrete usage a reader can copy. |
| X82 | render / `guides/state-store.mdx` | gaps | P2 | M | render has no testing guide or testing example anywhere in its doc set, while two peer adapter libs (langgraph, ag-ui) each ship a dedicated guides/testing.mdx. signalStateStore() and RenderSpecComponent are eminently unit-testable (set('/path', v) then assert the rendered output / store.get()). Add a short testing section (here or a new guides/testing.mdx) with one runnable spec: build a spec, mount RenderSpecComponent in TestBed with a signalStateStore, mutate the store, and assert the projected component updates — bringing render to example parity with the other adapters on the testing dimension. |
| X83 | render / `guides/specs.mdx` | structure | P2 | S | render is the engine that powers chat generative UI, but the cross-link is one-directional: chat/guides/generative-ui.mdx links out to render (specs, views) while no render page links back to chat. render/api/views.mdx explicitly frames views() as 'rendered inline in the chat when the agent produces a JSON spec' yet never links to /docs/chat/guides/generative-ui. Add a 'Using this in chat' cross-link from the render generative-ui-relevant pages (specs.mdx and/or api/views.mdx) to chat's generative-ui guide so the render reader can find the agent-streaming consumer. |
| X84 | render / `getting-started/introduction.mdx` | clarity | P2 | S | Two different functions are presented as 'the registry constructor' across render and chat docs without reconciling them, which confuses a reader moving between libs. render/getting-started/introduction.mdx teaches only defineAngularRegistry({...}); chat (generative-ui, api/views) and render/api/views.mdx teach only views({...}); render/concepts/json-render-vs-a2ui.mdx imports defineAngularRegistry at the top but then uses views() in the example. Add one sentence in the render introduction's Registry section clarifying the relationship between defineAngularRegistry() and views() (which is canonical, and that views() is the chat-facing helper), and fix the json-render-vs-a2ui import/usage mismatch so a single example uses one consistent function. |
| X85 | chat / `guides/generative-ui.mdx` | clarity | P2 | S | The guide opens 'Generative UI lets your LangGraph agent return structured JSON specs...' and hardwires the example to provideAgent/injectAgent from @threadplane/langgraph, but ChatComponent and the generative-UI/JSON-spec detection are adapter-neutral and AG-UI also drives generative UI (ag-ui architecture.mdx documents live a2ui/generative-UI streaming over AG-UI). Reword the opening to 'your agent' (runtime-neutral) and add a one-line note that the same [views] path works with the AG-UI adapter, linking to /docs/ag-ui/guides/custom-events. Aligns terminology with the adapter-neutral Agent-contract framing used elsewhere and adds the missing chat -> ag-ui generative-UI cross-link. |
| X86 | langgraph / `guides/streaming.mdx` | gaps | P2 | S | The retry() example calls this.chat.submit() with no arguments to 're-stream using the same thread', but the quickstart and persistence pages only ever show submit({ message }). A reader gets stuck on what no-arg submit() does (resume? replay last input?). Add one sentence defining the no-argument submit() / reload() semantics here, since this is the first place the journey relies on it. |
| X87 | chat / `getting-started/installation.mdx` | structure | P2 | S | installation.mdx step 4 shows provideAgent({ assistantId, threadId }) with no apiUrl, relying on an apiUrl set in an earlier provider block on the same page -- but the two code blocks are presented as independent 'full working example' snippets, so a copy-paste reader gets a provider with no endpoint and the 'Chat renders blank / 404s the backend' troubleshooting entry is the only safety net. Make the step-4 snippet self-contained (include apiUrl) or add an inline note that apiUrl comes from the root provider shown in step 3. |

## Accuracy follow-ups (out of scope here — route to separate tasks)

| Lib | Page | Recommendation |
|---|---|---|
| chat | `getting-started/quickstart.mdx` | The 'Configure providers' step calls `provideAgent({ apiUrl: 'http://localhost:2024' })` with no `assistantId`. `provideAgent` in libs/langgraph/src/lib/agent.provider.ts throws `provideAgent: \`assistantId\` is required to construct the AGENT singleton.` when `assistantId` is undefined, so this top-level app.config.ts block fails at runtime. (The later component-level `provideAgent({ assistantId: 'chat', threadId: signal(null) })` then redundantly re-registers it.) Fix the app.config.ts block to include `assistantId`, e.g. `provideAgent({ assistantId: 'chat', apiUrl: 'http://localhost:2024', threadId: signal(null) })`, and remove the duplicate component-level provider so the page shows one coherent wiring. |
| chat | `getting-started/installation.mdx` | Section '3. Wire provideChat() and provideAgent()' shows `provideAgent({ apiUrl: environment.langgraphApiUrl })` with no `assistantId`. Per libs/langgraph/src/lib/agent.provider.ts, `provideAgent` throws when `assistantId` is undefined, so this snippet fails at runtime. Add `assistantId` to the section-3 block (e.g. `provideAgent({ assistantId: 'chat', apiUrl: environment.langgraphApiUrl })`). |
| chat | `components/chat-trace.mdx` | The 'Full component example' calls `chatAgent.steps()` and reads `step.id`, `step.state`, `step.name`, `step.output`. None of these exist on the runtime-neutral `Agent` contract (libs/chat/src/lib/agent/agent.ts exposes messages/status/isLoading/error/toolCalls/state/interrupt?/subagents?/events$ — no `steps`). The example will not compile against the `injectAgent()` return. Rewrite the example to drive `[state]` from a real source, e.g. map `chatAgent.toolCalls()` entries (which carry `name`, `args`, `status`, `result`) to TraceState, or use a local signal of trace rows. |
| chat | `a2ui/catalog.mdx` | The Tabs, Modal, Video, and AudioPlayer JSON examples use the flat component shape (e.g. `{"id": "info-tabs", "component": "Tabs", "tabs": [...]}`), but a2ui/overview.mdx explicitly states the parser requires the keyed-union form `{"component": {"Tabs": {...}}}` and that 'a flat `component: "Text"` shape' is NOT recognized by the current source. The earlier catalog examples (Text, TextField, DateTimeInput, Slider) correctly use the keyed-union form, so these four are internally inconsistent and would not parse. Rewrite the Tabs/Modal/Video/AudioPlayer JSON snippets into the keyed-union form (e.g. `{"id": "info-tabs", "component": {"Tabs": {"tabs": [...], "selected": {"path": "/activeTab"}}}}`) after confirming the exact prop nesting against the catalog component sources. |
| langgraph | `concepts/agent-architecture.mdx` | The Multi-Agent Angular example (lines 418-435) reads `tool.toolCallId`, `tool.state`, and `tool.input` off `toolCalls()` entries, but the runtime-neutral `ToolCall` (libs/chat/src/lib/agent/tool-call.ts) has only `id`, `name`, `args`, `status`, `result`, `error`. There is no `toolCallId`, `state`, or `input` field. Change `track tool.toolCallId ?? tool.name` -> `track tool.id`, `[status]="tool.state"` -> `[status]="tool.status"`, and `[input]="tool.input"` -> `[input]="tool.args"` so the template compiles against the real contract. |
| langgraph | `concepts/agent-architecture.mdx` | The 'completed calls' tab (lines 280-298) advertises `Each entry has: name, args, result, duration` and renders `{{ call.duration }}ms`. The `ToolCall` contract has no `duration` field (only id/name/args/status/result/error). Either remove the duration column from the template and the comment, or compute elapsed time from another source; as written the `{{ call.duration }}` binding renders `undefinedms`. |
| langgraph | `concepts/agent-architecture.mdx` | The Recursion-limits callout (line 527) tells readers to set `graph.compile(recursion_limit=50)`. In LangGraph `recursion_limit` is not a `compile()` argument; it is a run-time config key. Fix to `graph.invoke(input, config={"recursion_limit": 50})` (or note it goes in the run config / `RunnableConfig`). |
| langgraph | `guides/interrupts.mdx` | In the Multi-Step Approval Pattern, DeployApprovalComponent (line ~402) calls `this.agent.interrupts()`, but the LangGraph agent surface has no plural `interrupts()` method. The raw signal is `langGraphInterrupts()` (the neutral signal is the singular `interrupt()`). Change `allInterrupts = computed(() => this.agent.interrupts());` to `this.agent.langGraphInterrupts()` (or drop the line, since it is never used in the template). |
| langgraph | `guides/deployment.mdx` | The `retrySubmit` helper (line ~239) types its first param as `chat: ReturnType<typeof agent>`, but `agent` is never imported or defined on this page — the public factory is `injectAgent`. Change to `ReturnType<typeof injectAgent>` (and add the import) or use `LangGraphAgent` so the example compiles. |
| render | `api/define-angular-registry.mdx` | The "Internal Behavior" code block (lines 101-111) is stale and self-contradicting. It shows defineAngularRegistry returning only { get, names } and doing map.get(name) directly, but the real impl (libs/render/src/lib/define-angular-registry.ts) normalizes each entry into { component, fallback }, stores a Map<string, NormalizedEntry>, and returns { get: name => map.get(name)?.component, getFallback: name => map.get(name)?.fallback, names }. The shown snippet omits getFallback entirely, directly contradicting the method table at lines 29-43 that documents getFallback. Replace the snippet with the actual implementation including normalize() and getFallback, or delete the "Internal Behavior" section. |
| render | `api/define-angular-registry.mdx` | The documented Signature (lines 14-17) and componentMap parameter type (Record<string, AngularComponentRenderer>) are incomplete. The real defineAngularRegistry accepts Record<string, Type<unknown> \| RenderViewEntry> where RenderViewEntry is { component, fallback? }. The page documents getFallback() returning a per-entry fallback but never shows that this object input form is how a consumer configures a custom fallback. Update the signature/parameter table to include the RenderViewEntry object form. |

